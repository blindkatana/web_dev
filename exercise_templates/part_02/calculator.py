import sys


"""
Evalute the string as an arithmetic expression.

Input:
exp - The arithmetic expression.

Return:
The result of the arithmetic expression.
"""
def evaluate(exp):
    return

if __name__ == '__main__':
    # sys.argv is a list of the arguments to the python call. The first argument
    # should be the name of the file (calculator.py) and the other arguments
    # should be the flags and inputs for that file.
    script_name = sys.argv[0] # Unnecessary line.
    args = sys.argv[1:end]

    print evaluate(args[0])
